function resultadoResto(dividendo, divisor) {
    const resultado = dividendo / divisor;
    const resto = dividendo % divisor;
    console.log(`Divisão ${dividendo} por ${divisor}`)
    console.log(`Resultado: ${resultado}`);
    console.log(`Resto: ${resto}`);
};
resultadoResto(4,2)
resultadoResto(2,4)
resultadoResto(5,2)


